# TypeDrummer
TypeDrummer maps typed letters to synthesized percussion and sequences typed text as rhythmic patterns. It uses only the Web Audio API (no audio samples). Intended as a lightweight, editable synth-driven drum/sequencer demo.
